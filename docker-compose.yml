version: '3.8'
services:
  app:
    # Dockerfile이 현재 폴더에 있다고 가정하고 빌드 컨텍스트를 지정합니다.
    build: . 
    ports:
      - "8080:8080"
    # 👇 이 부분이 핵심입니다! 현재 폴더의 .env 파일을 읽어 변수를 컨테이너에 주입합니다.
    env_file:
      - ./.env 
    container_name: my-spring-app