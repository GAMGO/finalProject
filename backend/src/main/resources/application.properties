spring.config.import=optional:file:.env[.properties]

# === 기존 MySQL 설정 ===
spring.datasource.url=jdbc:mysql://localhost:3306/my_project_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# === (선택) PostgreSQL 설정 ===
# spring.datasource.url=jdbc:postgresql://${HOST}:${PORT}/${DBNAME}
# spring.datasource.username=${RENDER_SQL_ID}
# spring.datasource.password=${RENDER_SQL_PW}
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === JPA 기본 설정 ===
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
server.port=8080

# === 이메일 SMTP 설정 ===
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=minsulim820@gmail.com
spring.mail.password=aqfhvarjqyjazzof
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.connectiontimeout=5000

# === ✅ FastAPI 통계 갱신 연동 추가 ===
# FastAPI 서버 주소 (개발 환경)
stats.fastapi.base-url=http://localhost:8000
# 연결 및 읽기 타임아웃 (밀리초)
stats.fastapi.connect-timeout=2000
stats.fastapi.read-timeout=3000
