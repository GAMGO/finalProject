드가자

pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121  # (CUDA 버전 맞춰서)
pip install transformers datasets accelerate
pip install fastapi uvicorn[standard]
pip install streamlit
